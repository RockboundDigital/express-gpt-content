import { css } from "lit";

export const style = css`
    .app {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    .container {
        margin: 24px;
        display: flex;
        flex-direction: column;
    }
`;
